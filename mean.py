# will this list contain both int and float number?
def mean(num_list):
	if not num_list:
		return 'No input'

	num_sum, res = 0, 0
	for n in num_list:
		num_sum += nu

	res = float(num_sum)/len(num_list)

	return res




